@model IEnumerable<PrimeSimulateur.Models.Travail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TravailId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.surface)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Logement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TravailId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.surface)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Logement.adresse)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TravailId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TravailId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TravailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
